# 粉笔网关：可选。设置后将覆盖代码内默认 Cookie；留空或删除该行则使用 api/fenbi_gateway.py 内默认。
# FENBI_COOKIE=sess=你的sess; userid=你的userid

# cre_image 图生图（可选）：设置后 reference_images 的 image_url 仅允许这些主机；不设置则任意 https 图片 URL 均可。
# CRE_IMAGE_ALLOWED_URL_HOSTS=cdn.example.com
